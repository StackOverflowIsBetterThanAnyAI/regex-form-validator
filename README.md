# regex form validator

A basic form validator project written in HTML, CSS and plain JavaScript making use of JavaScript RegEx which confirms live if the user uses a valid email address and password.
